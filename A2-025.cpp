#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){
	int numc, numr, posc, pocr, n;
	cin >> numr;
	cin >> numc;
	int empty = numr * numc;
	vector<vector<int>> grid(numr, vector<int>(numc, 0));
	cin >> posr;
	cin >> posc;
	cin >> n;
	int tempr, tempc;
	for(int i = 0; i < n; i++){
		cin >> tempr;
		cin >> tempc;
		grid[tempr][tempc] = 100;

	}
}
